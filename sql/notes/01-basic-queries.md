# Basic SQL Queries

## Summary

Basic SQL queries are used to retrieve data from tables.

## Main Keywords

- SELECT
- FROM
- WHERE
- ORDER BY
- LIMIT

## Filter
Use WHERE

## Unique
Use DISTINCT

## Count จำนวน
User COUNT
- count(*) = total row in that table
- if we use count(DISTINCT order_status) = number of unqiue order status